# frozen_string_literal: true

RSpec.describe BibliographicMatchKey do
  it "has a version number" do
    expect(BibliographicMatchKey::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
